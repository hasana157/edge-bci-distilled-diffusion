# Processed Data

This directory is generated by preprocessing and should not contain committed arrays.

Expected generated files may include:

- `train_eeg.npy`, `val_eeg.npy`, `test_eeg.npy`
- `train_labels.npy`, `val_labels.npy`, `test_labels.npy`

Raw and processed data arrays are ignored by Git to keep the repository lightweight.
